{
  "name": "sales-ocr-api",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "build": "bun install && bun run db:gen && bun run db:migrate:deploy",
    "start": "bun run src/index.ts",
    "db:up": "docker compose up -d",
    "db:migrate": "bunx prisma migrate dev",
    "db:migrate:deploy": "bunx prisma migrate deploy",
    "db:gen": "bunx prisma generate --no-hints",
    "db:seed": "bunx prisma db seed",
    "db:studio": "bunx prisma studio --browser none",
    "db:reset": "bunx prisma migrate reset"
  },
  "dependencies": {
    "@google/genai": "^2.9.0",
    "@hono/zod-openapi": "^1.1.5",
    "@prisma/adapter-pg": "^7.8.0",
    "@prisma/client": "^7.8.0",
    "@scalar/hono-api-reference": "^0.9.25",
    "@types/node": "^24.10.1",
    "@types/pg": "^8.15.6",
    "argon2": "^0.44.0",
    "doppler": "^1.0.0",
    "dotenv": "^17.2.3",
    "hono": "^4.10.7",
    "zod": "^4.1.13"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "bun-types": "^1.3.14",
    "prisma": "^7.8.0"
  }
}
